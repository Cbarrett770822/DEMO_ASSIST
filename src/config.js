// Client-side configuration
const config = {
  // API base URL - use relative path for proxy to work correctly
  apiUrl: '/api',
  
  // Development mode settings
  development: {
    disableFallback: true,
    debugMode: true
  }
};

export default config;
